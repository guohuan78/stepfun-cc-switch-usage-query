# StepFun 新版 Step Plan 用量查询：CC Switch 配置说明

版本：v1.1
日期：2026-08-19
适用对象：新版按月度 Credit 计量的 Step Plan

## 1. 功能说明

本配置通过 StepFun 网页端 Dashboard 接口查询新版 Step Plan 的月度 Credit 剩余比例，并在 CC Switch provider 卡片中显示：

- 已用比例
- 剩余比例
- 总比例（100%）

新版套餐不使用旧版的 5 小时或周限额字段。接口核心字段位于：

    response.planCreditRateLimit.subscriptionCreditLeftRate

该接口返回剩余比例，不直接返回套餐的绝对 Credit 总量。因此本脚本显示百分比，不显示 M Credits 的绝对数值。

## 2. 配置入口

在 CC Switch 中：

1. 打开目标 StepFun provider 的 Usage Query / 用量查询。
2. 打开 Enable Usage Query。
3. 类型选择 Custom。
4. 将本目录中的 stepfun-cc-switch-usage-query.js 全部粘贴到脚本框。
5. 按第 3 节说明手动替换脚本中的两个标记。
6. 点击 Test Script，成功后保存。

CC Switch 的自定义查询脚本使用 request 和 extractor 配置对象。本仓库不依赖 CC Switch 的自定义变量替换能力，凭证采用粘贴前手动替换的方式：

<https://github.com/farion1231/cc-switch-website/blob/main/public/docs/en/2-providers/2.5-usage-query.md>

## 3. 参数填写

### Access Token：完整的 Oasis-Token

必须从已登录的 StepFun 网页会话中获取完整的 Oasis-Token。

方法 A：Application / 应用程序面板

1. 打开并登录 <https://platform.stepfun.com>。
2. 按 F12 打开开发者工具。
3. 进入 Application / 应用程序。
4. 打开 Storage → Cookies → https://platform.stepfun.com。
5. 找到 Oasis-Token，复制 Value 的完整内容。

方法 B：Console

在 StepFun 页面对应的 Console 执行：

    (() => {
      const item = document.cookie
        .split(";")
        .map((s) => s.trim())
        .find((x) => x.startsWith("Oasis-Token="));
      return item
        ? decodeURIComponent(item.slice("Oasis-Token=".length))
        : "未找到 Oasis-Token";
    })()

复制结果时保持单行。Token 中如果本来含有三个连续的点号，不要自行删除或拆分。

### User ID：网页的 web_id

这里使用网页的 web_id，不是账户的数字 oasis_id。

在 Application / 应用程序 → Local Storage → https://platform.stepfun.com 中找到 web_id，或者在 Console 执行：

    localStorage.getItem("web_id")

两个值必须来自同一个浏览器会话。

### Base URL

填写：

    https://platform.stepfun.com

### API Key

本查询不依赖 Step API key。可以留空，或保留 provider 原本的 API key。

## 4. 手动替换脚本

先将脚本粘贴到文本编辑器中，只替换下面两个标记：

    PASTE_OASIS_TOKEN_HERE
    PASTE_WEB_ID_HERE

- PASTE_OASIS_TOKEN_HERE 替换为完整 Oasis-Token。
- PASTE_WEB_ID_HERE 替换为 web_id。
- 只替换双引号内的标记，保留双引号。
- 不要添加 Bearer、Cookie: 或 Oasis-Token= 前缀。
- 替换完成后直接粘贴到 CC Switch；不要把带真实凭证的版本提交回仓库。

当前脚本的关键请求头如下：

    ({
      request: {
        url: "https://platform.stepfun.com/api/step.openapi.devcenter.Dashboard/QueryStepPlanRateLimit",
        method: "POST",
        headers: {
          "Accept": "application/json",
          "Content-Type": "application/json",
          "Oasis-Token": "PASTE_OASIS_TOKEN_HERE",
          "Oasis-Webid": "PASTE_WEB_ID_HERE",
          "Oasis-appID": "10300",
          "Oasis-Platform": "web",
          "Oasis-Language": "zh-CN",
          "User-Agent": "cc-switch/1.0"
        },
        body: "{}"
      },

      extractor: function (response) {
        var root = response || {};

        if (root.data && typeof root.data === "object") {
          root = root.data;
        }

        var limit =
          root.planCreditRateLimit ||
          root.plan_credit_rate_limit ||
          root;

        var left = limit.subscriptionCreditLeftRate;

        if (left === undefined) {
          left = limit.subscription_credit_left_rate;
        }

        left = parseFloat(left);

        if (isNaN(left)) {
          return {
            isValid: false,
            invalidMessage:
              "未找到 planCreditRateLimit.subscriptionCreditLeftRate"
          };
        }

        // 接口通常返回 0~1；兼容 0~100 的返回格式。
        if (left > 1) {
          left = left / 100;
        }

        left = Math.max(0, Math.min(1, left));

        var remaining = Math.round(left * 10000) / 100;

        return {
          isValid: true,
          planName: "Step Plan 月度 Credit",
          used: Math.round((100 - remaining) * 100) / 100,
          total: 100,
          remaining: remaining,
          unit: "%",
          extra: ""
        };
      }
    })

## 5. 显示结果含义

假设接口返回：

    {
      "planCreditRateLimit": {
        "subscriptionCreditLeftRate": 0.875
      }
    }

CC Switch 会显示：

    已用：12.5%
    总量：100%
    剩余：87.5%

## 6. 常见错误

### 解析配置失败：Exception generated by QuickJS

- 不要粘贴 Markdown 代码围栏。
- 脚本必须整体用括号包裹。
- Access Token 必须是一行，不能含换行符。
- 如果采用手动替换，确认 Token 粘贴后仍是单行。
- 不要把替换后的真实凭证版本保存到仓库。

### 401 Unauthorized / oasis-token is embezzled

- 重新登录 StepFun 网页。
- 同时复制新的 Oasis-Token 和新的 web_id。
- 确认 User ID 填的是 web_id，不是 oasis_id。
- 确认没有添加 Bearer、Cookie: 或其他前缀。

### 未找到 subscriptionCreditLeftRate

确认使用的是当前脚本。新版字段在 planCreditRateLimit 对象内，不能只读取顶层的 subscriptionCreditLeftRate。

### 测试成功但 provider 卡片没有显示

- 确认已打开 Enable Usage Query。
- 确认配置已经保存。
- 确认该 provider 当前处于 Active 状态。
- 点击 provider 卡片上的刷新按钮，或等待 Auto Query Interval 触发。

## 7. 绝对 Credit 用量的边界

QueryStepPlanRateLimit 只提供剩余比例。若要显示类似：

    已用 23.4M / 总额 400M

还需要额外查询套餐档位和 QueryStepPlanUsages 用量明细，再进行汇总。当前这个 CC Switch 单请求脚本版本不做绝对 Credit 换算，避免因套餐档位变化产生错误结果。

## 8. 官方页面

- StepFun 用量页面：<https://platform.stepfun.com/plan-usage>
- StepFun Step Plan：<https://platform.stepfun.com/step-plan?channel=step-dev>
- CC Switch 用量查询文档：<https://github.com/farion1231/cc-switch-website/blob/main/public/docs/en/2-providers/2.5-usage-query.md>

文档和仓库脚本不包含任何实际 Token。Oasis-Token 属于网页登录会话凭证，不要提交到公开仓库或共享文档中。
